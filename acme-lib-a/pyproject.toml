[project]
name = "acme-lib-a"
version = "0.0.0"
description = ""
requires-python = ">=3.8"
dependencies = [
    "acme-lib-b",
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
